version: 2.1

jobs:
  deploy:
    docker:
      - image: arvindr226/alpine-ssh
    steps:
      - run: ssh -i $SSH_PRIVATE_KEY -oStrictHostKeyChecking=no -v $USER@$DNS "sudo mkdir -p /home/ubuntu/sangam"

workflows:
  deploy-to-ec2:
    jobs:
      - deploy
